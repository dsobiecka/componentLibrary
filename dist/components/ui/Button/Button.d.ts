type ButtonProps = {
    onClick: () => void;
    disabled?: boolean;
    label: string;
};
export declare const Button: ({ onClick, disabled, label }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export {};
